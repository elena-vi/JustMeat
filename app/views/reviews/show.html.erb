<p>
  <strong>Description:</strong>
  <%= @review.description %>
</p>

<p>
  <strong>Rating:</strong>
  <%= @review.rating %>
</p>

<%= link_to 'Edit', edit_review_path(@review) %> |
<%= link_to 'Delete', @review, method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', "/restaurants/#{@review.restaurant_id}" %>
